@model List<Application.ViewModels.GenderViewModel>
@{
    ViewData["Title"] = "Generos";
}

<div class="container-fluid">

    <div class="row mb-3">
        <div class="col-3 offset-9">
            <a asp-controller="Gender" asp-action="Create" class="btn btn-outline-success">Crear nuevo genero</a>

        </div>

    </div>

    <div class="row">

        @if (Model == null || Model.Count == 0)
        {
            <h2>No hay Generos</h2>
        }
        else
        {
            <div class="col-4 offset-4">
                <ul>
                    @foreach (Application.ViewModels.GenderViewModel item in Model)
                    {

                        <li class ="d-flex justify-content-between align-items-center">
                            <span class="h4">@item.Name</span>
                            <a asp-controller="Gender" asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm btn-outline-info">Editar</a>
                            <a asp-controller="Gender" asp-action="Delete" asp-route-id="@item.Id" class="btn btn-sm btn-outline-danger">Borrar</a>
                        </li>
                           
                    }
                </ul>
            </div>
        }

    </div>
</div>

